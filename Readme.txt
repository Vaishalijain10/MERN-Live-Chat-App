Live Chat App - Basic Setup

folder - frontend -> react

folder - backend -> node, express and mongodb


deployment  -> 
1. update server.js
2. .env file
3. fontend -> command -> npm run build
4. "build" : "npm install --legacy-peer-deps --prefix backend && npm install --legacy-peer-deps --prefix frontend && npm run build --prefix frontend"
5. "start": "react-scripts --openssl-legacy-provider start",
    "build": "react-scripts --openssl-legacy-provider build",
6. 

